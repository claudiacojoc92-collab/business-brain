/**
 * Dev-only founder id for the M2.1 slice (decision 4). Just enough to attach evidence and
 * render a reflection without wiring real auth. Real authentication is a later, separate
 * concern — do NOT build auth on top of this. Matches the seeded dev founder.
 */
export const DEV_FOUNDER_ID = '01HDEV000000000000000FOUNDER';
