# Dockerfile.migrate — Flyway image for Railway prod migration (V001..V062 baked, no host mount).
# Connection/schema config come from env (FLYWAY_URL/USER/PASSWORD/SCHEMAS) via Railway variables.
FROM flyway/flyway:10-alpine
COPY database/migrations /flyway/sql
CMD ["info"]
